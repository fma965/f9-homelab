apiVersion: v1
kind: ConfigMap
metadata:
    name: traefik-providers
    namespace: traefik
data:
    homeassistant.yaml: |-
        http:
          routers:
            homeassistant:
              entryPoints:
                - "websecure"
              rule: "Host(`home.f9.casa`)"
              tls: {}
              service: homeassistant
              middlewares:
                - traefik-default-headers@kubernetescrd

          services:
            homeassistant:
              loadBalancer:
                servers:
                  - url: "http://10.0.10.216:8123"
    unraid.yaml: |-
        http:
          routers:
            unraid:
              entryPoints:
                - "websecure"
              rule: "Host (`unraid.f9.casa`)"
              tls:
              service: unraid
              middlewares:
                - traefik-default-headers@kubernetescrd

          services:
            unraid:
              loadBalancer:
                servers:
                  - url: "http://f9-nas.internal"
    adguardhome.yaml: |-
        http:
          routers:
            adguardhome:
              entryPoints:
                - "websecure"
              rule: "Host (`adguard.f9.casa`)"
              tls:
              service: adguardhome
              middlewares:
                - traefik-default-headers@kubernetescrd

          services:
            adguardhome:
              loadBalancer:
                servers:
                  - url: "http://192.168.1.1:8080"
    plex.yaml: |-
        http:
          routers:
            plex:
              entryPoints:
                - "websecure"
              rule: "Host (`plex.f9.casa`)"
              tls:
              service: plex
              middlewares: traefik-default-headers@kubernetescrd

          services:
            plex:
              loadBalancer:
                servers:
                  - url: "http://f9-nas.internal:32400"
    frigate.yaml: |-
        http:
          routers:
            frigate:
              entryPoints:
                - "websecure"
              rule: "Host (`cctv.f9.casa`)"
              tls:
              service: frigate
              middlewares:
                - traefik-default-headers@kubernetescrd


          services:
            frigate:
              loadBalancer:
                servers:
                  - url: "http://f9-nas.internal:5000"
    zipline.yaml: |-
        http:
          routers:
            zipline:
              entryPoints:
                - "websecure"
              rule: "Host (`i.f9.casa`)"
              tls:
              service: zipline
              middlewares: traefik-default-headers@kubernetescrd
          services:
            zipline:
              loadBalancer:
                servers:
                  - url: "http://f9-nas.internal:8092"
    proxmox-hv1.yaml: |-
        http:
          routers:
            proxmox-hv1:
              entryPoints:
                - "websecure"
              rule: "Host (`hv1.f9.casa`)"
              tls:
              service: proxmox-hv1
              middlewares: traefik-default-headers@kubernetescrd
          services:
            proxmox-hv1:
              loadBalancer:
                servers:
                  - url: "https://10.0.40.1:8006"
    proxmox-hv2.yaml: |-
        http:
          routers:
            proxmox-hv2:
              entryPoints:
                - "websecure"
              rule: "Host (`hv2.f9.casa`)"
              tls:
              service: proxmox-hv2
              middlewares: traefik-default-headers@kubernetescrd
          services:
            proxmox-hv2:
              loadBalancer:
                servers:
                  - url: "https://10.0.40.2:8006"
    proxmox-hv3.yaml: |-
        http:
          routers:
            proxmox-hv3:
              entryPoints:
                - "websecure"
              rule: "Host (`hv3.f9.casa`)"
              tls:
              service: proxmox-hv3
              middlewares: traefik-default-headers@kubernetescrd
          services:
            proxmox-hv3:
              loadBalancer:
                servers:
                  - url: "https://10.0.40.3:8006"
    proxmox-backup-server.yaml: |-
        http:
          routers:
            pbs:
              entryPoints:
                - "websecure"
              rule: "Host (`pbs.f9.casa`)"
              tls:
              service: pbs
              middlewares: traefik-default-headers@kubernetescrd
          services:
            pbs:
              loadBalancer:
                servers:
                  - url: "https://f9-nas.internal:8007"
    minecraft.yaml: |-
        http:
          routers:
            minecraft:
              entryPoints:
                - "websecure"
              rule: "Host (`mc.f9.casa`)"
              tls:
              service: minecraft
              middlewares: traefik-default-headers@kubernetescrd
          services:
            minecraft:
              loadBalancer:
                servers:
                  - url: "http://10.0.10.32:8100"
    openwebui.yaml: |-
        http:
          routers:
            openwebui:
              entryPoints:
                - "websecure"
              rule: "Host (`chat.f9.casa`)"
              tls:
              service: openwebui
              middlewares: traefik-default-headers@kubernetescrd
          services:
            openwebui:
              loadBalancer:
                servers:
                  - url: "http://f9-nas.internal:8480"
    forgeui.yaml: |-
        http:
          routers:
            forgeui:
              entryPoints:
                - "websecure"
              rule: "Host (`sd.f9.casa`)"
              tls:
              service: forgeui
              middlewares:
                - traefik-default-headers@kubernetescrd

          services:
            forgeui:
              loadBalancer:
                servers:
                  - url: "http://f9-nas.internal:7400"
    backrest.yaml: |-
        http:
          routers:
            backrest:
              entryPoints:
                - "websecure"
              rule: "Host (`backup.f9.casa`)"
              tls:
              service: backrest
              middlewares:
                - traefik-default-headers@kubernetescrd

          services:
            backrest:
              loadBalancer:
                servers:
                  - url: "http://f9-nas.internal:9898"
    forgejo.yaml: |-
        http:
          routers:
            forgejo:
              entryPoints:
                - "websecure"
              rule: "Host (`git.f9.casa`)"
              tls:
              service: forgejo
              middlewares: traefik-default-headers@kubernetescrd
          services:
            forgejo:
              loadBalancer:
                servers:
                  - url: "http://f9-nas.internal:3001"
    remote.yaml: |-
        http:
          routers:
            remote:
              entryPoints:
                - "websecure"
              rule: "Host (`remote.f9.casa`)"
              tls:
              service: remote
              middlewares: traefik-default-headers@kubernetescrd
          services:
            remote:
              loadBalancer:
                servers:
                  - url: "https://10.0.10.10:443"
    nanokvm.yaml: |-
        http:
          routers:
            nanokvm:
              entryPoints:
                - "websecure"
              rule: "Host (`kvm.f9.casa`)"
              tls:
              service: nanokvm
              middlewares:
                - traefik-default-headers@kubernetescrd

          services:
            nanokvm:
              loadBalancer:
                servers:
                  - url: "http://192.168.30.10:80"
    komodo.yaml: |-
        http:
          routers:
            komodo:
              entryPoints:
                - "websecure"
              rule: "Host (`komodo.f9.casa`)"
              tls:
              service: komodo
              middlewares: traefik-default-headers@kubernetescrd
          services:
            komodo:
              loadBalancer:
                servers:
                  - url: "http://f9-nas.internal:9120"
    01.gameservers.f9.casa.yaml: |-
        http:
          routers:
            gameservers-01:
              entryPoints:
                - "websecure"
              rule: "Host (`01.gameservers.f9.casa`)"
              tls:
              service: gameservers-01
              middlewares: game-gameserverheaders@kubernetescrd
          services:
            gameservers-01:
              loadBalancer:
                servers:
                  - url: "http://10.0.10.31:8080"
    02.gameservers.f9.casa.yaml: |-
        http:
          routers:
            gameservers-02:
              entryPoints:
                - "websecure"
              rule: "Host (`02.gameservers.f9.casa`)"
              tls:
              service: gameservers-02
              middlewares: game-gameserverheaders@kubernetescrd
          services:
            gameservers-02:
              loadBalancer:
                servers:
                  - url: "http://10.0.10.32:8080"
    03.gameservers.f9.casa.yaml: |-
        http:
          routers:
            gameservers-03:
              entryPoints:
                - "websecure"
              rule: "Host (`03.gameservers.f9.casa`)"
              tls:
              service: gameservers-03
              middlewares: game-gameserverheaders@kubernetescrd
          services:
            gameservers-03:
              loadBalancer:
                servers:
                  - url: "http://10.0.10.33:8080"
    passbolt.yaml: |-
        http:
          routers:
            passbolt:
              entryPoints:
                - "websecure"
              rule: "Host (`passbolt.f9.casa`)"
              tls:
              service: passbolt
              middlewares:
                - traefik-default-headers@kubernetescrd
          services:
            passbolt:
              loadBalancer:
                servers:
                  - url: "http://f9-nas.internal:5680"
