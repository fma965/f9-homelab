---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: lldap
  annotations:
    kubernetes.io/ingress.class: traefik-external
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`lldap.f9.casa`)
      kind: Rule
      services:
        - name: lldap
          port: 80
      middlewares:
        - name: default-headers
          namespace: traefik
