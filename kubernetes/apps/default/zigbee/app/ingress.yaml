---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: zigbee
  annotations:
    kubernetes.io/ingress.class: traefik-external
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`zigbee.f9.casa`)
      kind: Rule
      services:
        - name: zigbee
          port: 80
      middlewares:
        - name: default-headers
          namespace: traefik

