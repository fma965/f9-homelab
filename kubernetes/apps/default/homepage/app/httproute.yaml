apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: homepage
spec:
  parentRefs:
    - name: external
      namespace: network
      sectionName: https
  hostnames:
    - "f9.casa"
  rules:
    - backendRefs:
        - name: homepage
          port: 3000